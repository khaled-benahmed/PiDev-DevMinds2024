<form method="post" action="{{ path('app_allergie_delete', {'id': allergie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allergie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
